services:
  # Database
  postgres:
    image: postgres:15-alpine
    container_name: precious-vault-db
    environment:
      POSTGRES_DB: precious_vault
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - precious-vault-network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: precious-vault-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - precious-vault-network

  # Backend API
  backend:
    build:
      context: ./precious-vault-backend
      dockerfile: Dockerfile
    container_name: precious-vault-backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./precious-vault-backend:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - precious-vault-network

  # Celery Worker
  celery:
    build:
      context: ./precious-vault-backend
      dockerfile: Dockerfile
    container_name: precious-vault-celery
    command: celery -A config worker -l info
    volumes:
      - ./precious-vault-backend:/app
    env_file:
      - .env
    environment:
      - SKIP_MIGRATIONS=true
    depends_on:
      - postgres
      - redis
    networks:
      - precious-vault-network

  # Celery Beat Scheduler
  celery-beat:
    build:
      context: ./precious-vault-backend
      dockerfile: Dockerfile
    container_name: precious-vault-celery-beat
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    volumes:
      - ./precious-vault-backend:/app
    env_file:
      - .env
    environment:
      - SKIP_MIGRATIONS=true
    depends_on:
      - postgres
      - redis
    networks:
      - precious-vault-network

  # Channels (WebSocket)
  channels:
    build:
      context: ./precious-vault-backend
      dockerfile: Dockerfile
    container_name: precious-vault-channels
    command: daphne -b 0.0.0.0 -p 9000 config.asgi:application
    volumes:
      - ./precious-vault-backend:/app
    ports:
      - "9000:9000"
    env_file:
      - .env
    environment:
      - SKIP_MIGRATIONS=true
    depends_on:
      - postgres
      - redis
    networks:
      - precious-vault-network

  # Frontend
  frontend:
    build:
      context: ./precious-vault-frontend
      dockerfile: Dockerfile
    container_name: precious-vault-frontend
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:8000/api
      - VITE_WS_URL=ws://localhost:9000/ws
    depends_on:
      - backend
    networks:
      - precious-vault-network

  # Admin Frontend
  admin-frontend:
    build:
      context: ./precious-vault-admin
      dockerfile: Dockerfile
    container_name: precious-vault-admin
    ports:
      - "3001:80"
    environment:
      - VITE_API_URL=http://localhost:8000/api
    depends_on:
      - backend
    networks:
      - precious-vault-network

volumes:
  postgres_data:

networks:
  precious-vault-network:
    driver: bridge
